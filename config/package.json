{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "A simple weather app with geolocation support",
  "main": "js/script.js",
  "scripts": {
    "start": "http-server -p 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cityjs": "latest"
  },
  "config": {
    "cityjs": {
      "dataDir": "./data"
    }
  },
  "devDependencies": {
    "http-server": "latest"
  }
}
